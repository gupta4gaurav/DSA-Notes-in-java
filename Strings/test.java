public class test {
    public static boolean isPalindrome(String str) {

        for(int i = 0; i > (str.length()/2); i++)
        {
            if ( str.length(i) == str.length(n-1-i))
            {

            }
        }
    }
    public static void main(String[] args) {
        
    }
    
}
